/**
 * DEPRECATED: dev_issues table eliminata
 * Stub per evitare errori di compilazione
 */

export function useDevIssues() {
  return {
    issues: [],
    isLoading: false,
    error: null,
    mutate: () => Promise.resolve(),
  };
}
